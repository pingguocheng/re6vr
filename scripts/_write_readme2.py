#!/usr/bin/env python3
"""Regenerate README.md's "current state" section after the 2026-09-22 geometry pass.

Everything here is written by hand and written at once, because the previous
README was produced by scripted string surgery and ended up half mojibake. Write
with an explicit encoding; never let PowerShell touch a file with Chinese in it.

Usage: python scripts/_write_readme2.py
"""
import io
import os

ROOT = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))

HEAD = """# RE6 VR Mod — 接手指南

> **给新会话**：一切都在磁盘上，与对话无关。本文件是唯一入口，先读完这一节再动代码。

## 30 秒了解现状

给 Steam 版 Resident Evil 6（32 位 DX9 / MT Framework 2.x）做的 VR 注入层。
**游戏画面已经能出现在 Quest 3 头显里**，双眼各画一份、读回、提交全链路打通，
每帧提交无失败。当前在打磨"屏幕多大、放在哪、比例对不对"。

```
代码     C:\\re6vr\\
部署     C:\\Program Files (x86)\\Steam\\steamapps\\common\\Resident Evil 6\\d3d9.dll
         （219136 字节）+ openxr_loader.dll
```

> ⚠️ Steam 校验文件或重装游戏会删掉这两个 DLL，需要重新 `scripts\\deploy.bat`。

## 三条命令

```bat
scripts\\build.bat      编译 32 位 d3d9.dll
scripts\\deploy.bat     部署到游戏目录（需要写权限，工作区外要提权）
scripts\\uninstall.bat  卸载
```

**启动必须走 Steam**（`BH6.exe` 直接跑会 `Failed to initialize Steam.` 然后退出）。

## 屏幕设置（2026-09-22 重做）

| 变量 | 默认 | 作用 |
|---|---|---|
| `RE6VR_SCREEN_DIST` | `4.0` | 玩家到屏幕的距离（米）。**现在真的生效** |
| `RE6VR_SCREEN_WIDTH` | `4.6` | 屏幕宽度（米），高按 16:9 自动算 |
| `RE6VR_IPD_SCALE` | `0` | `0`=两眼共用视点（消除重影），`1`=真实眼距 |
| `RE6VR_SCREEN_FOLLOW` | `1` | 头部偏转超过约 45° 时把屏幕重新锚定到正前方 |

默认值下屏幕张角 **59.8° × 35.8°**，日志里每帧会打印实测值：

```
frustum eye0: submitting 59.8 x 35.8 deg (mode 0), panel 4.60 x 2.59 m at 4.00 m,
              slice 998x2148 aspect 0.465, submitted tan-aspect 1.778
```

`RE6VR_FRUSTUM_ASPECT_MODE` 是诊断开关，保持 `0`：`0` = 视锥取面板矩形（提交区域
16:9，画面完整），`1` = 视锥取视口比例（画面只占视口中间一条，上下黑边）。
两种模式离线渲染出来的差别见 `_logs/cmp_mode0.png` / `cmp_mode1.png`。
"""

BODY = """## 最重要的技术结论（读代码看不出来的那些）

### 1. `IDirect3DSurface9` 的 vtable 比 `d3d9.h` 多一个槽 ← 最关键

运行时对象在槽 11 有一个**不属于公开接口的保留槽**，后面全部后移一位：

```
d3d9.h 文档      实际运行时
GetDesc  = 11     Reserved11 = 11
LockRect = 12     GetDesc    = 12
UnlockRect = 13   LockRect   = 13
GetDC    = 14     UnlockRect = 14
                  GetDC      = 15
```

我按文档写槽位时，每一次 "LockRect" 实际调用的是 `GetDesc`，它往调用者的
`D3DLOCKED_RECT`（8 字节）里写 32 字节的 `D3DSURFACE_DESC` —— 踩掉调用者栈帧。
这就是那些 NULL `pBits`、`structured exception`、以及"跳到 0x16"的全部来源。
**修正已在 `src/d3d9_min.h` 落地**，用 `build\\_copytest\\copyprobe.exe` 逐槽位实测确认。

### 2. MT Framework 不允许在帧内创建 D3D9 纹理

帧内 `CreateTexture` 会让引擎以 `ERR09 : Unsupported function.` 直接致命退出。
所有表面必须在**第一帧渲染之前**建好。

### 3. 后台缓冲不能直接作为读回源

`StretchRect(backbuffer → texture)` 返回 `D3DERR_INVALIDCALL`，
`LockRect(backbuffer)` 抛异常。可用路径是：
**同尺寸同格式的 `StretchRect` → `GetRenderTargetData` → `LockRect`**。

### 4. 绝不能包装 `IDirect3D9Ex`

它比 `IDirect3D9` 多 4 个槽。现在 Ex 工厂直接透传，只补丁设备**自己那份 vtable
副本**的 `Present` / `EndScene` / `Reset`。

### 5. 会聚点必须在屏幕前方

`ipd_scale=0` 时若把会聚点放在屏幕平面上，面板视空间深度为 0，frustum 表达不了
→ 全黑。会聚点必须在屏幕前方，且**距离必须等于 `screen_dist`**（见坑 29）。

### 6. MinHook 已移除

它的 trampoline 在第一次 `Present` 就崩（fault offset 0x4C89008B）。
改为直接补丁设备 vtable 副本。

### 7. 眼睛 dump 的真实磁盘布局（2026-09-22 踩到）

`dump_eye_image()` 把整张 swapchain 纹理 `CopyResource` 到 staging，再
`Map(subresource(0, eye, 1))` 取一个 array slice，然后**按纹理宽度 `d.Width`
写行、按 `d.Height` 迭代行数**。所以文件是：

```
宽 = sc_width（单眼宽，实测 998）
高 = sc_height x 2（两个 slice 上下堆叠，实测 2148*2 = 4296）
```

**不是左右并排的 1996×2148。** 按 1996 宽去读会把两个 slice 的行交错混在一起，
得到一张"看起来挺合理"的假图 —— 我据此错误推断了两轮，白绕很久。
正确工具：`scripts\\eye_raw.py`（按真实布局切开）、`scripts\\eye_cover.py`（量覆盖）。

### 8. 视口比例与面板比例无关，且这不影响观感

单眼 slice 是 998×2148（比例 0.465，竖长条），源图是 16:9（1.778）。
缓冲区里画面被"竖直拉伸"约 3.8 倍，方格测试图能直接看出来。
但**运行时是按提交的 `fov` 把图像矩形映射到显示的**：提交 59.8°×35.8°（tan 比例
1.778）时，缓冲里 0.465 的比例在显示上就是 1.778，方格是方的。
所以 mode 0 下画面不变形、不裁切 —— 缓冲的像素比例只是"像素不是方的"，不是错误。
"""

LESSONS = """## ⚠️ 血泪教训

1. **眼睛 dump 的布局必须先确认再解读**（见结论 7）。布局猜错会让你在错误数据上
   建立一整套自洽的错理论。
2. **`place_panel()` 的调用点被脚本改写删掉过三次**，每次症状都是全黑。改完确认这行还在：
   ```cpp
   if (!panel_placed) place_panel(real_eye, fwd);
   ```
3. **不要用"字符串切片"批量改写 C++**。用行号精确替换，改完立刻读回来确认。
4. **渲染结果必须实测**，不能靠读代码推断。用离线的两个自检（见下）。
5. **"数学正确"≠"结果正确"**。关键路径要打日志比对。
6. **PowerShell 写含中文的文件会破坏编码**。用 Python + `encoding="utf-8"` 写。
7. **别在缓冲区像素比例上做判断**。缓冲区是竖长条不代表显示是竖长条；
   要看"提交的 fov 角度"和"面板的角尺寸"。
8. **测试里的硬编码采样点会随着可调参数失效**。经典自检里"面板外应该是黑"的
   ±0.95 NDC 采样点，在视距变成可配置后就落到画面内容上了，于是总报 FAIL。
   现在探测点直接按源图 UV 取（象限中心），不再依赖面板多大。
9. **Steam 启动的游戏不继承 shell 的环境变量。** 这是本项目所有 `RE6VR_*` 开关的
   共同陷阱，也是 Stage 2 第一次侦察白跑的原因：设了 `RE6VR_CAPTURE_VS=1` 再启动，
   日志里连"探针已安装"都没有 —— 而**探针静默不安装看起来和"游戏里没有矩阵"一模一样**。
   现在关键开关支持**标记文件**（放在游戏目录旁边），Steam 启动也能用。
   凡是"这个开关没生效"的怀疑，先确认进程里到底有没有读到它。
10. **告警/统计日志不能每帧打。** 计时日志曾写成"计数器归零后也算一次报告"，
    结果 90 秒产生 8182 行 `readback` 日志（1.3 MB），把真正有用的信息全淹了。
    报告与归零必须绑在同一个条件上。
"""

STATUS = """## 已知遗留

- OpenXR 首次初始化会阻塞游戏线程一次（runtime 冷启动约 12 秒，已延后 1.2 秒）。
- **读回成本已实测（2026-09-22 真机）：18.17 ms/帧，约 51 fps。** 日志行为
  `compositor: readback 18.17 ms/frame` / `copy thread ~51.6 frames/s`。
  这是整个管线里最贵的一环，也是 Stage 2 的性能预算基线：真双视角要把场景渲染
  两遍，必须在这个数字之上再算。
- **性能瓶颈在读回，不在眼睛缓冲。** 每帧 3840×2160 要走
  `StretchRect → GetRenderTargetData → LockRect → memcpy`（3.7 MB/帧出总线）；
  D3D11 那边只是"一次 Clear + 一个 quad"，214 万像素在 4070 Ti 上约 1 GB/s 填充，
  相对总线传输可忽略。**帧率有问题时先看这个，不要先缩眼睛缓冲。**
  已加计时日志 `compositor: readback X ms/frame` / `copy thread ~X frames/s`，
  但需要合成激活（即需要头显），离线量不到，待真机。
- `RE6VR_SWAPCHAIN_W/H` 可把单眼 slice 从推荐的 998×2148 缩到约 545×800：按
  **运行时自身像素密度**换算（998×59.8/110、2148×35.8/96），画质不变、像素数降到 21%。
  **风险**：非推荐尺寸能否被 Virtual Desktop 接受尚未验证（需会话，无头显测不了）；
  不接受时 `xrCreateSwapchain` 会失败并打日志，去掉变量即可恢复。
  注意**不要**用 561 高——那会让竖直像素密度掉到与水平相同（约 16 px/度），
  低于运行时对这个张角的建议密度，会糊。
- **Stage 2（真双视角立体渲染）已开始，卡在"取到相机矩阵"这一步。** 见下。
- **Stage 3（头部瞄准 / 姿态注入）尚未开始。**

## Stage 2：真双视角立体渲染（进行中）

目标：把"一张平面画面贴到世界里"换成"两个真有视差的相机"，即每只眼看到略微不同的
几何。**难点不在渲染，在于必须让 MT Framework 用它自己的相机渲染两次**——引擎闭源，
矩阵不公开。

**当前假设**：MT Framework 每帧通过 `SetVertexShaderConstantF` 上传视图/投影矩阵，
所以在这一层拦截即可拿到。若成立，第二个眼的做法是：拦截 `SetVertexShaderConstantF`，
把视图矩阵按 IPD 平移（视图矩阵是刚体变换，平移一行即可），让引擎把场景画两遍。

**已验证的工具**：`src/matrix_probe.cpp`，用 `RE6VR_CAPTURE_VS=1` 开启。它挂在
设备 vtable 的 `SetVertexShaderConstantF`（槽 94）上，对每批常量做分类：

| 判据 | 结论 |
|---|---|
| 3×3 部分正交归一（三行单位长、两两垂直）+ 第四行有平移 | `VIEW` |
| 第四列除透视项外为 0，第四行为 (0,0,1,0) | `PROJ` |
| 两者都不满足但 3×3 明显非正交、且带平移 | `VIEW*PROJ`（合并矩阵，很多引擎只传这个） |

**分类器已用已知矩阵标定过**（不是猜的）：

```bat
set RE6VR_CAPTURE_VS=1
build\harness.exe build\d3d9.dll 3 1280 720 vsmatrix
:: 期望日志：cap: reg 0 looks like VIEW (score 1.00)
::           cap: reg 8 looks like PROJ (score 1.00)
```

**第一次真机采集结果（2026-09-22 21:00，标题画面）**：

```
cap: SetVertexShaderConstantF(reg 1, 4 vec4)     <- 投影矩阵，独立上传
cap: reg 1 = [1.3580 0 0 0][0 2.4142 0 0][0 0 -1.0001 -1.0000][0 0 -1.0001 0]
cap: SetVertexShaderConstantF(reg 0, 128 vec4)  <- 128 寄存器大块，视图矩阵在其中
cap: reg 5 = [1 0 0 42][0 1 0 -30][0 0 1 -100][1 0 0 0]
```

**结论**：

1. **两个矩阵都在**，且都走着色器常量路径 —— 不需要改挂 `SetTransform`。
2. `2.4142 = 1/tan(22.5°)`（90° 透视）是投影矩阵的指纹。
3. **视图矩阵是列主序**：寄存器里每个 float4 是一**列**，所以平移
   `(42, -30, -100)` 出现在寄存器 5/6/7 的 `.w` 分量上。按行读会看不出平移。
   （这也是最初把它误判成 `VIEW*PROJ` 的原因，分类器已改为同时接受两种布局。）
4. 每帧各上传一次；`reg 0,128` 那个大块里除了视图矩阵还有别的东西，
   需要靠原始寄存器 dump 定位边界（探针已加）。
5. **标题画面下视图矩阵完全不动**（四次快照平移都是 `(42,-30,-100)`），
   所以第一批数据里拿不到"相机移动时矩阵怎么变"。探针已改为**相机平移超过
   0.05 m 就重新采样**，进关卡走动即可拿到序列。

**第二次真机采集结果（2026-09-22 21:06，关卡内走动）**：

寄存器布局**不是固定的**，随着色器变化：

```
VIEW 出现在 reg 1, 4, 6, 9, 27
PROJ 出现在 reg 1, 5, 13
```

视图矩阵采样（世界坐标，证明跟踪的是真相机）：

```
frame  2412:   3990.270  -1185.540    626.589
frame  3056:  -3376.760   -882.441  -2757.520
frame  3595:  -3576.390   -617.601  -1941.830
frame  5918:   1286.120   -425.041   -417.585
```

**这决定了两件事**：

1. **不能靠固定寄存器号注入**。必须**按特征识别**"这一批里哪个是视图矩阵"
   （正交归一 3×3 + 第四行有平移）。固定 reg 号会同时漏掉大部分 draw 并破坏
   该位置上的其他数据。
2. 投影矩阵紧跟在视图矩阵后面 4 个寄存器（view reg N，proj reg N+4），
   这是可用的结构性线索。

配套工具 `scripts/matrix_map.py` 自动解析 `cap:` 日志、重建布局并列出相机轨迹，
不用手搓 grep。

**安全实验第一次真机结果（2026-09-22 21:09）—— Stage 2 最大的未知已解决**：

> 用户反馈：**没有崩溃**，画面出现**水平复制错位**（卡普空 logo 看起来像"卡卡普普空空"）。

这直接回答了两个问题：

1. **帧内改写视图矩阵是安全的** —— 引擎没有像帧内 `CreateTexture` 那样 `ERR09` 退出。
2. **画面按预期整体水平偏移** —— 正是每眼偏移需要的行为，而不是只偏一部分
   （那只偏一部分才说明命中了副相机）。

**但日志暴露了一个我自己的 bug**：游戏**复用同一个常量缓冲区**，导致原地改写
**累积**：

```
shift: reg 4 translation 0.000 -> 0.030
shift: reg 4 translation 0.030 -> 0.060     <- 同一个缓冲区被改了第二次
```

所以请求的 3 cm 实际变成了 6 cm，而用户看到的"每个字母复制一份"正是这个
6 cm 错位造成的。已修：**每帧每个缓冲区只改一次**（按指针去重），并加每帧
汇总日志 `N view matrix buffer(s) shifted`——**正确的运行应该恒为 1**，
数字变大就是这个 bug 复发。

本地验证：`shift: frame 0: 1 view matrix buffer(s) shifted by 0.030 m (total 1 since start)`。

**副作用**：这次意外反而证明了偏移量是可控且可见的——3 cm 在标题画面视距下
产生的字母错位肉眼明显，说明**每眼 ±3 cm（IPD 的一半）的偏移量级是合适的**。

**第二次安全实验（2026-09-22 21:18）—— 找到了"复制"的真因**：

用户反馈"依旧 cacapcom"（字母仍然成对复制）。日志揭示了两层原因，都不是崩溃：

1. **每帧不止一个视图矩阵。** 场里有主相机 + 阴影/反射等副相机，我把**全部**
   都偏移了，于是同一物体被几个不同相机各画一遍 → 复制。
2. **原地累加会叠加。** 游戏每帧重建缓冲区但**每帧上传两次**视图矩阵，
   我按帧去重只挡住其一，于是 3 cm 变成 6 cm。

**修法（两层）**：

- 改写方式从"累加"改为**锚定**：记下引擎自己的值，每次写
  `锚定值 + 偏移`。这样**无论被调用多少次都不会叠加**。
- 锚定**按视图矩阵索引分别保存**：一帧里有多个矩阵时，共用一个锚点会被它们
  轮流覆盖，导致偏移时有时无。
- 新增**选择模式**（标记文件第二行）：`-1` = 全部，`-2` = 最后一个（推测是主相机），
  `0/1/…` = 指定第几个。默认 `-1` 保持与首次实验一致。

**第三次安全实验（2026-09-22 21:22）—— 找到了"选错矩阵"的真因**：

用户反馈"否"（复制没消失）。日志显示 `pick=-2` 选中的东西是：

```
shift: frame 316 view#0 reg 1 translation 1920.0000 -> 1920.0300
```

**1920 = 屏幕宽度**。`reg 1` 不是相机，而是**投影矩阵**——投影矩阵的 3×3 部分
也是正交归一的（只有 xy 缩放），所以我的"正交归一 + 有平移"判据把它当成了视图矩阵。
**真相机一次都没被碰过**，画面当然照旧。

原始寄存器给出了铁证（帧 16 的 128 寄存器批）：

```
reg 1..4   = 投影矩阵，平移是 (1/3840, 1/2160) = (0.000260417, 0.000462963)
reg 5..8   = 视图矩阵，平移 (42, -30, -100)
reg 8..11  = 同一个视图矩阵的转置（引擎两种布局都传）
```

**教训**：投影矩阵的"平移"是**屏幕尺寸的倒数**，是个普普通通的小数字，
任何"世界量级"过滤都挡不住。真正的判据是它配着**近单位的 3×3**。

**修法**：判据识别"近单位 3×3 + 极小平移"→ 归为投影；选择逻辑排除
`VIEW^T`（那是同一矩阵的转置副本，计入索引会让 `pick` 的含义随批次布局漂移）。

**离线验证（`vsdecoy` 模式，不必跑游戏）**：harness 上传"诱饵 + 真相机"同批，
复刻游戏布局：

```
cap: reg 0 looks like VIEW^T      <- 诱饵
cap: reg 4 looks like VIEW        <- 真相机
cap: frame 0 view#0 (batch reg 0) translation 1920.0000 -30.0000 4430.0000  <-- SHIFTED
```

`pick=0` 现在选中的是真相机。配套 `scripts/pick_check.py` 可在**已有日志**上
复算选择逻辑，不用再启动游戏。

**第四次安全实验（2026-09-22 21:29）—— 选中的是 UI 精灵，不是相机**：

用户反馈"依旧 capcapcom"。日志显示：

```
cap: frame 16 view#0 (batch reg 0) translation 0.0000 0.0000 1.0000   <-- SHIFTED
cap: frame 16 view#1 (batch reg 0) translation 0.0300 0.0000 1.0000
```

**平移 = `(0, 0, 1)`，而游戏的世界坐标在数千单位**（实测相机平移 4375..4468）。
所以 `pick=0` 选中的是**平移到原点附近的 UI/精灵矩阵**，不是相机。而且
`view#1` 与 `view#0` 只差我刚写进去的 0.03 —— 引擎会回读常量块，于是**同一份数据
作为第二个候选再次出现**，一个被改一个没改，这个 0.03 的错位就是"capcapcom"。

**三次失败的共同根因**：我的"正交归一 + 有平移"判据太宽，把它先后误判成
相机的东西依次是 —— 屏幕倒数型投影（平移 0.00026）、视口型矩阵（平移 1920）、
UI 精灵（平移 1）。

**现在的判据（两条都必须满足）**：

1. **平移是世界量级**（≥50 且 ≤1e6）：相机在世界里，UI 在原点附近。
2. **3×3 是非平凡旋转**：相机会旋转世界，而 identity / 纯屏幕缩放不会 ——
   这一条才挡得住携带 `(1920, 1080)` 这类"平移"的矩阵（那是屏幕尺寸，不是位置）。

**离线验证（`scripts/pick_check.py`）**：改判据不再需要跑游戏。用真实游戏日志复算：

```
frame 16-18:  reg4(PROJ/UI,1) reg6(PROJ/UI,1)   <- 平移=1 的伪影，已排除
frame 1517:   reg1(VIEW,4468)  translation (3267, -413.9, -3019)
frame 1875:   reg9(VIEW,4468)  translation (518.5, -1169, -4281)
```

**每一帧恰好一个真视图矩阵**，`pick=0` 因此是明确的。

**第五次安全实验（2026-09-22 21:33）—— 判据终于选对了矩阵，但复制仍在**：

```
cap: frame 1500 view#0 (batch reg 27) translation 331.72 -286.09 -549.06
                                      rot0 -0.7597 -0.1263 -0.6379
```

旋转是真正的视角旋转、平移连续变化 —— **这次选中的确实是相机**。但用户反馈
"还复制"。

**新的怀疑（此前从未验证）**：复制也许**本来就在**，与我的平移无关。证据是日志里
**两个线程**（16392 与 23000）都在处理同一个视图矩阵：

```
[t=16392] cap: frame 1500 view#1 (batch reg 27) translation 331.75 ...
[t=23000] shift: frame 1501 ... anchor 331.7167 + 0.030 m
```

两个线程各自提交视图矩阵，符合"**游戏自己每帧把场景画两遍**"（双眼渲染）。
若成立，我们的合成器只是把已经复制的画面照搬出去 —— 那样再调矩阵判据永远无效。

**验证手段（新加）**：`re6vr_dump_at.txt`（内容 = 帧号）让合成器在指定帧把
**游戏原始帧**导出成 `re6vr_frame_at.bgra`。这是第一次能直接看"游戏交给我们的
画面里有没有复制"，而不是从头显的拷贝去反推。已部署帧 1500（标题画面）。

**注意**：帧导出需要 OpenXR 会话（合成器启动才有帧），所以无头显时测不了。

**教训**：前四轮都在调"哪个矩阵是相机"，却从未验证过"复制是不是我的平移造成的"。
这个前提一旦不成立，四轮实验全部无效。

**第六次实验（2026-09-22 21:38）—— 基线确认：复制与本 Mod 无关**：

在**完全不改写任何矩阵**的配置下，用户反馈"还复制"。这是决定性结论：

> **复制是游戏自己产生的，我们的合成器只是照搬。** 前四轮调"哪个矩阵是相机"
> 方向完全错了。

**同一轮里发现的两个问题**：

1. **我的日志在撒谎。** `pick = -1` 时 `chosen` 恒为真，而日志把 `chosen` 当作
   "已改写"来标注，于是**基线运行看起来像在改引擎**。已修：只有真正写入
   （`chosen && 实验开启`）才标 `<-- WRITTEN`。日志必须可信到能区分"基线"和"实验"。
2. **两个线程各自上传略微不同的视图矩阵**：

   ```
   frame 1543 reg 27 translation 331.30 -284.51 -550.14
   frame 1544 reg 27 translation 331.47 -284.23 -550.18
   ```

   同一帧内两套相机，符合"**游戏自己把场景渲染两遍**"。若它们画进同一个后台缓冲，
   画面自带幽灵 —— 这就是复制的机制。

**帧导出的价值**：`re6vr_dump_at.txt` 现在接受**一串帧号**（一次运行抓多个画面，
因为标题画面只出现很短一段，单帧号猜一次要跑一趟）。帧 1500 已确认是自动保存
提示、文字干净 —— 但那不是好样本（UI 提示可能不参与双份渲染）。需要**标题画面**
那一帧：`re6vr_frame_at_<帧号>.bgra`。

**注意**：帧导出需要 OpenXR 会话，无头显时测不了。

**第七次实验（2026-09-22 21:41）—— 复制不在游戏帧里，也不在眼睛图里**：

用 `re6vr_dump_at.txt` 一次导出 8 帧，找到标题画面（帧 1400/1600/2200）并用
`scripts/ghost_find.py` 正式检测（找"远离零点、且明显高于自身邻域"的局部极大，
而不是像早期版本那样把平滑衰减当成复制）：

```
game frame 1400/1600/2200 : NO duplication detected
left eye image (1996x2148) : NO duplication detected
两眼逐字节相同（ipd_scale=0 下应当如此）
```

**因此提交给头显的东西是干净的**：游戏帧干净、左眼图干净、两眼一致。
既然画面里没有复制，复制只能来自**提交之后的环节**（运行时/头显侧）。

**当前最强嫌疑：换链比例与提交 FOV 差 1.91 倍**

| | 宽高比 |
|---|---|
| 换链（每眼） | 1996 × 2148 → **0.93** |
| 提交的 FOV | 59.8° × 35.8° → **1.778** |

这条以前被我用"运行时按 FOV 映射图像矩形，所以缓冲像素不是方的也没关系"
**解释掉了，从没实测**。若 Virtual Desktop 的畸变网格是按换链尺寸生成的，
这个失配就可能让画面竖直压缩、横向重复 —— 正是复制的样子。

**测试**：`re6vr_swapchain.txt`（内容 `<宽> <高>`，高填 0 = 自动匹配 16:9）现在
可用标记文件设置，因为**环境变量对 Steam 启动无效**。已设为 `1996 0` → 1996×1123。
若复制消失，则确认是换链比例问题；若依旧，则问题在运行时侧，需要换验证路径。

**第八次实验（2026-09-22 21:45）—— 上一轮测试是坏的，且"只渲染一只眼"的怀疑被排除**：

**1. 换链比例测试完全无效（我的 bug）**

```
openxr: swapchain override from re6vr_swapchain.txt: 1996 x 0 (height 0 = match 16:9)
openxr: swapchain size override -> 1996x64
```

`1996x0`（高 0 = 自动算 16:9）本意是 1996×1123，但**"解析派生值"被放在了"下限钳位"之后**，
高度先被钳成 64，于是造出一个高 64 像素的换链。日志看起来还挺正常。
已修：派生值必须在钳位之前算。**教训：派生与钳位/校验的顺序要有意识，不能顺手写。**

**2. 两只眼都在正常工作**

```
slice0 sha256 : 2d9ae626...
slice1 sha256 : 44897d3b...
slice0 == slice1: False     差异 6,360,699 字节
```

两个 array slice 内容不同 → 两眼各写了内容，不存在"只渲染一只眼、复制两份"。
（两个 dump **文件**曾经哈希相同，那是上一轮的坏换链造成的，不能作为"两眼相同"的证据。）

**3. 眼睛 dump 的正确布局再确认**：文件大小 17149632 同时等于 998×4296×4 与
1996×2148×4，**光看字节数无法分辨**。按 998×4296 读会得到**斜切错位**的画面
（文字被截断、logo 撕裂）；按 1996×2148 读则是完整正确的标题画面。
所以正确布局是 **1996 宽 × 2148 高，两个 slice 垂直堆叠**（即纹理宽 1996）。
`WriteFile(..., d.Width * 4, ...)` 里的 `d.Width` 是 1996 —— 这与日志一致。

### 9. 眼睛图是"更多"像素，但画面却更糊 —— 分辨率被丢在了别处（2026-09-22 查官方文档后找到）

**官方结论**（[Vulkan Documentation Project, Stereo Viewport & Scissor Management](https://vulkan.lunarg.com/doc/view/latest/windows/antora/tutorial/latest/OpenXR_Vulkan_Spatial_Computing/04_Dynamic_Rendering/03_stereo_viewport_scissor.html)）：

> The runtime knows the physical curvature of the lenses. It provides a viewport that is
> typically **larger** than the physical display panel resolution (**often 1.4x**) to
> account for the detail lost during the "barrel distortion" warping process.

也就是说推荐的每眼分辨率**本身已含约 1.4 倍过采样**，不是浪费。

**真正的糊来自分辨率链路，与 FOV 无关**：

```
游戏后台缓冲 3840x2160
   ↓ StretchRect 降采样
staging 1280x720          <-- 这里是硬编码的 1280（丢掉 89% 像素）
   ↓ 再放大
眼睛纹理 ~1996 宽
```

`create_shared_surface()` 里 `const uint32_t work_w = w > 1280 ? 1280 : w;` —— 4K 画面被
压到 720p，合成器再把 720p **放大**进眼睛纹理。这条链路里丢掉的像素永远回不来。

**修法**：staging 尺寸默认跟随眼睛纹理宽度（不再固定 1280），且不超过源分辨率。
可用 `re6vr_capture_w.txt`（或 `RE6VR_CAPTURE_W`）覆盖，日志会打印
`capture staging WxH ... (N% of the source pixels)`。

**教训**：一直盯着 FOV 和视锥比例找"糊"，而糊是**分辨率**问题。
先量链路每一级的分辨率，再谈几何。

**第九次实验（2026-09-22 21:53）—— 两眼分离度：数据上是 0**：

```
converge eye0: player (-0.03 -1.31 0.00) -> eye (-0.03 -1.31 0.00)
converge eye1: player ( 0.03 -1.31 0.00) -> eye (-0.03 -1.31 0.00)   <- 收敛到同一点
```

两眼从 ±0.03 的**不同**实际位置收敛到**完全相同**的相机位置，两张眼睛图 MD5 一致
（`89066db99a4d4c92`）。**从我们的输出看，两眼分离度为 0，不存在"隔得太远"。**

若头显里仍有水平错开，它只能来自**提交之后的环节**——运行时的位姿重投影：
运行时按每眼的预测位姿对图层重投影，而两眼位姿天生相差一个 IPD。

**对照实验**：`re6vr_ipd_scale.txt`（0 = 两眼共用视点，1 = 真实眼距）现在可用标记文件
设置（环境变量对 Steam 启动无效）。测 0 → 0.25 → 1.0 就能判定看到的错开量级是否就是
正常立体视差。

**顺带记下一个观察**：日志里 `frustum` 行的面板距离会在 4.00 m 与 3.11 m 之间跳
（`screen (-0.09 -0.01 -4.01)`），因为 `yaw_follow` 在头部转动后重新锚定屏幕，
而重新锚定的距离基准与首次放置不同。这不是 bug 但会让"屏幕多大"随动作变化，
值得后续统一。

**第十次实验（2026-09-22 22:0x）—— 重影的量级指向运行时位姿重投影**：

用户描述：错开约**一个字母宽**。据此定量：

| 量 | 值 |
|---|---|
| 标题字幕字距（1280 宽画面内） | ≈ 9 px/字母 |
| 换算到眼睛纹理（1996 宽） | ≈ 26 px |
| **IPD 63 mm 在 4 m 屏幕处的视差**（1280 宽画面） | ≈ **66 px** |

两眼的真实位姿差所产生的视差，换算到眼睛缓冲**恰好也是一个字母宽量级**。
而我们提交的两眼是**同一张图**（MD5 一致），所以这个错位**不是我们渲染出来的**。

**推断**：运行时对图层做**位姿重投影**时，假设图像是**从每只眼各自的位置**渲染的，
按每眼位姿采样。而我们把相机放在了**两眼中间**（`ipd_scale = 0` 的设计意图），
于是运行时额外引入了一个 IPD 量级的错位。

**这解释了为什么"消除重影"的招反而制造了重影**：为了去掉几何视差，我把相机放到中间，
却让运行时的采样假设失配。

**测试**：`re6vr_ipd_scale.txt` 改为 **1**（每眼用真实位姿），使我们的渲染与运行时的
采样假设一致。若重影消失或显著减轻，则确认该推断，并把它设为默认。

**第十一次实验（2026-09-22 21:59）—— 重影与两眼分离度无关（关键负面结果）**：

`re6vr_ipd_scale.txt = 1` 确实生效了：

```
converge eye0: player (-0.03 -1.96 0.00) -> eye (-0.03 -1.96 0.00)
converge eye1: player ( 0.03 -1.96 0.00) -> eye ( 0.03 -1.96 0.00)   <- 两眼各自真实位姿
```

把两眼分离度从 **0 改成 63 mm**，用户反馈"**没有变化**"。因此：

> **重影与两眼分离度无关。** 既不是立体视差，也不是运行时按位姿重投影造成的。

**这条排除了两个此前的主要假设**，价值很高。剩下的可能只有两类：

1. 我们提交的图像本身在**那一帧**就有重影（此前的"眼睛图干净"结论测的是第 800 帧，
   与用户看的画面不是同一时刻）；
2. 运行时做了与位姿无关的几何处理（例如按换链尺寸与提交 FOV 的失配去采样）。

**下一步：把两个导出对齐到同一帧。** `re6vr_dump_at.txt` 的第一项现在同时作为
**眼睛图的导出帧**，于是同一次运行会产出**可对比的一对**（游戏原始帧 + 该帧提交的眼睛图）。
这一对能直接判定重影是在**合成环节**引入的，还是在**提交之后**。

**第十二次实验（2026-09-22 22:02）—— 我们的绘制路径全部排除，改做纯色判别**：

用户反馈"**依旧**，而且**从第一步能显示画面时就是这样**"。这句时间是关键：
它说明重影不是今晚任何改动引入的，而是**从最开始就存在**的东西。

**今晚已确证的事实**：

| 检查项 | 结果 |
|---|---|
| 游戏原始帧（多帧） | **干净**（`ghost_find.py` 无局部极大） |
| 像素着色器 | 单次采样，UV 就是 [0,1] |
| 采样器寻址 | `D3D11_TEXTURE_ADDRESS_CLAMP`（不可能纹理回绕） |
| 图层提交 | 1 个投影图层、2 个视图、各用 `imageArrayIndex = i` |
| IPD 0 ↔ 63 mm | **无变化** |
| 分辨率 | 已修复（`capture staging 1996x1122`，清晰度应改善） |

**落在我们这边的环节全部排除了。**

**还发现一个矛盾**：`ipd_scale=0` 与 `ipd_scale=1` 两次运行导出的眼睛图
**MD5 完全相同**（`89066db99a4d4c92`）。而 `ipd_scale=1` 时两眼视图必然不同
（±0.03 的相机位置差）。所以**眼睛图导出很可能一直没在区分两个 slice**，
此前所有基于"两眼图"的判断都不可靠。

**判别实验（已部署）**：`re6vr_test_solid.txt` 让合成图层显示**纯绿色**，
完全忽略游戏画面。纯色没有内部细节可供复制，所以：

- 头显里看到**两块绿** → 合成路径把面板画了两次
- 看到**一块绿 + 一份游戏画面** → 头显侧显示了游戏窗口/桌面镜像（与我们无关）
- 看到**一块绿** → 图层本身正常，问题在别处

**第十三次实验（2026-09-22 22:1x）—— 纯色实验必须分左右眼不同色**：

用户反馈：**"你得给左右眼不同颜色给我判断，左右都一样我只能看出是一块绿色"**。

这个反馈本身就是一个重要结论：**同一个颜色时，人无法区分"每眼各看到自己那一块"与
"两眼看到同一块"** —— 而这正是实验要回答的问题。实验设计有缺陷。

`re6vr_test_solid.txt = pereye` 现在让**左眼纯红、右眼纯绿**。判读方式：

| 头显里看到 | 结论 |
|---|---|
| **融合成黄色/橙黄** | 两眼各得自己的图层 —— 合成路径正确，问题在别处 |
| 同一只眼里同时有红和绿，或两块色带 | 某只眼同时收到了另一只眼的图层（**这就是重影**） |
| 全红或全绿 | 两眼拿到同一个 slice |

**顺带**：用户此前只看到"一块绿色"，至少说明**图层只出现一次**——如果是两块绿，
他会说"两块绿"。所以"我们的图层被画两次"这一假设基本被排除。

**第十四次实验（2026-09-22 22:08）—— 异色实验做了，但红通道我写错了**：

用户看到"**左边一大截蓝色，右边一大截绿色，中间一小块混合**"。

**先认错**：我本意是左红右绿，但把 `0x00FF0000` 当 ARGB 去移位写进 BGRA 纹理，
得到的内存值是 `0x000000FF`——**红通道根本没写进去**，所以显示成蓝色。
已改为**按名字显式取通道**（`0x00RRGGBB`），不再在两种约定之间转换。

**这个观察的有效信息**（颜色标错不影响结论）：

1. **两眼各自看到了自己的图层**（左眼一色、右眼一色），没有被对调或共用。
2. **两眼图像显著错开**——如果几乎重合，看到的应该是"全部混合"而不是"两大块+中间一小块"。

第 2 点与 `ipd_scale=1` 下应有的几何（4 m 处 0.06 m 眼距 ≈ 0.86°）**对不上**：
0.86° 在 59.8° 的视场里只有约 1.5%，不该出现"两大截"。

**又一次发现自检在骗我**：`quad check` 打出的角点全是
`ndc=(0.00 0.00) w=0.00`，另一帧是 `ndc=(-5.20 -5.53)` ——
**这个自检的数学是坏的**，此前用它判断"四角落在视口内"的结论不可信。

**同时确认**：帧 300 已是 logo 之后的画面、帧 500 是淡出黑场，
所以此前"游戏帧干净"的样本**并不包含 Capcom logo 段**。
已把导出帧改到 60/90/120/150/200/250（logo 刚出现）。

**第十五次实验（2026-09-22 22:12）—— Capcom logo 帧干净，游戏排除了**：

把导出帧调到 logo 刚出现的阶段（60/90/120/150/200/250），帧 150/200/250 正是
**Capcom logo**。视觉上"CAPCOM"清晰无复制；数值上三帧都是：

```
frame 150/200/250: strongest local maximum prominence +0.000 -> NO duplication
```

**所以：游戏自己每帧并没有把画面画两遍。**
（此前"游戏帧干净"的样本不含 logo 段，这次的样本才对上。）

**因此重影只能来自**：我们提交的眼睛图，或头显侧。

**唯一无歧义的测试（已部署）**：`re6vr_test_solid.txt = half` 让合成器显示一张
**左半红 / 右半绿 / 中间一条白竖线**的图案（烘进一张 64×2 小纹理，因为我们的着色器
直接采样纹理、不吃 uniform 颜色）。

| 头显里看到 | 结论 |
|---|---|
| **两条带**（红 \| 绿，中间一条白线） | 眼睛图只被画了一次 —— 合成路径正确 |
| **四条带**（红绿红绿） | 眼睛图被画了两次（**重影的机制**） |
| 白线**出现多条** | 画面被水平平铺 |
| **全红**或**全绿** | 两眼拿到同一个 slice |

**为什么必须用图案而不是纯色**：纯色的两份拷贝看起来和一份完全一样——
这正是上一轮"一块绿色"无法给出结论的原因。

**第十六次实验（2026-09-22 22:20）—— 每只眼单独看都是对的，错位在两眼之间**：

用"左半红 / 右半绿 / 中央白线"的图案（`re6vr_test_solid.txt = half`）问到两个关键答案：

1. **闭上一只眼看** → "中央一条白线、左红右绿"：
   **每只眼拿到的图像本身是正确的。**
2. **轮流闭眼比较白线位置** → "左眼白线偏左，右眼白线偏右"：
   **两眼图像之间存在位移**（而非内部重复）。

而日志确认 `ipd_scale = 0` 生效（两眼收敛到同一点 `(-0.03 -1.96)`），
**我们提交的两眼数据完全相同、内部视差为 0**。所以这个位移**不是我们渲染出来的**。

**这解释了"capcapcom"的真身**：每只眼单独看都是清晰的 "capcom"，
合起来因为两眼错位而变成 "capcapcom"。**它不是"图像里有两份"，而是"两眼各一份、位置不同"。**

**唯一还没排除的机制：我们的 swapchain 是"一个纹理、两个 array slice"**。
如果运行时没有正确按 `imageArrayIndex` 分 slice，就可能把两个 slice 都送进同一只眼
或按错误的基准对位——症状恰好是"每眼都对、合起来错位"，且改任何渲染参数都无效。

**已实现第二种合法布局**（`re6vr_two_swapchains.txt`）：`arraySize = 1`，
**每只眼一个独立 swapchain**，`imageArrayIndex` 恒为 0。
这需要把提交路径改成成对 acquire/release 与按眼选择 swapchain，已完成。
离线回归全过（两种自检 + reset）。

**看图工具**：`scripts/eye_sbs.py` 把两眼按头显的配对方式**并排**输出。
磁盘上眼睛 dump 的两个 slice 是**上下堆叠**的，但"有没有重影/错位"是**水平**判断，
必须左右放着看：

```bat
:: 离线自检（每眼一个文件）
python scripts\eye_sbs.py build\re6vr_real_eye0.bgra build\re6vr_real_eye1.bgra _logs\sbs.png
:: 游戏内采集（单文件双 slice）
python scripts\eye_sbs.py "C:\...\Resident Evil 6\re6vr_eye_left.bgra" _logs\sbs_game.png 998 2148
```

其余侦察/分析脚本：

| 脚本 | 用途 |
|---|---|
| `matrix_map.py` | 解析 `cap:` 日志，重建寄存器布局与相机轨迹 |
| `eye_raw.py` | 按真实布局（上下堆叠）切开眼睛 dump |
| `eye_cover.py` | 量面板在单眼视口里的覆盖与黑边 |
| `eye_sbs.py` | 两眼并排成一张图 |
| `compare.bat` | 一次渲染多种屏幕配置做对比 |



要回答的问题，按重要性排序：

1. **矩阵在哪些寄存器？**（`cap: reg N looks like VIEW/PROJ`）
2. **是分开的 VIEW+PROJ，还是合并的 VIEW*PROJ？** 合并的话必须先把两者分解出来。
3. **每帧上传几次？** 出现多次说明有多个相机/阴影 pass/后处理，得挑出主相机那一次。
4. **能不能在帧内改写而不崩？** 这是最大风险：本项目已知 MT Framework 对帧内
   `CreateTexture` 会直接 `ERR09` 致命退出，对常量改写是否同样敏感**未知**。
   **必须先在单眼上试**（把视图矩阵平移一点点，看画面是否偏移而不是崩溃），
   确认安全再做双眼。

**风险提示**：真双视角意味着场景渲染两遍（D3D9 那边翻倍），加上本文档上文测到的
读回瓶颈，帧率可能不够。先做单眼平移验证，再评估性能。

## 不加头显就能验证渲染（务必用这两个）

```bat
:: 1) 经典合成自检：四象限方向 + 双眼都画到了
set RE6VR_SELFTEST=1
build\\harness.exe build\\d3d9.dll 6 1280 720

:: 2) 真实几何自检：998x2148 竖长条 + 16:9 源图，双眼图落盘
set RE6VR_SELFTEST=1
set RE6VR_SELFTEST_REAL=1
build\\harness.exe build\\d3d9.dll 4 1280 720
:: 产物 build\\re6vr_real_eye0.bgra / eye1.bgra
```

第 2 个是本次新加的：它用真实切片比例渲染，所以能离线量出"面板占多少视野、
有没有裁切"。配套脚本：

```bat
python scripts\\eye_raw.py   build\\re6vr_real_eye0.bgra _logs\\real 998 2148 4
python scripts\\eye_cover.py build\\re6vr_real_eye0.bgra 998 2148
python scripts\\eye_png.py   build\\re6vr_real_eye0.bgra _logs\\eye.png 2 998 2148
```

一条命令跑三种屏幕配置对比：`scripts\\compare.bat`。

## 环境变量

| 变量 | 作用 |
|---|---|
| `RE6VR_SELFTEST=1` | 离线渲染自检，不需要头显 |
| `RE6VR_SELFTEST_REAL=1` | 真实几何自检 + 双眼图落盘（配合上一个用） |
| `RE6VR_DUMP_FRAME=n` | 第 n 帧导出游戏帧与双眼图（默认 800，0=关） |
| `RE6VR_NO_XR=1` | 纯直通：只记日志、不合成 |
| `RE6VR_FRUSTUM_ASPECT_MODE` | 诊断用，保持 0（见上） |
| `RE6VR_SWAPCHAIN_W` / `_H` | 单眼 slice 尺寸覆盖，实验性（见"已知遗留"） |
| `re6vr_swapchain.txt` | 同上，内容 `<宽> <高>`，高填 0 = 自动匹配 16:9（Steam 启动用这个） |
| `re6vr_capture_w.txt` | 抓帧 staging 宽度，0/缺省 = 跟随眼睛纹理（决定清晰度，见结论 9） |
| `RE6VR_CAPTURE_VS=1` | Stage 2 侦察：分类引擎上传的顶点着色器矩阵（只读） |
| `re6vr_capture_vs.txt` | 同上，放在游戏目录。**Steam 启动用这个**（见坑 9） |
| `re6vr_shift_view.txt` | Stage 2 安全实验：第一行 = 平移米数，第二行 = 选择哪个视图矩阵<br>`-1` 全部（默认）／`-2` 最后一个（推测主相机）／`0,1,…` 指定序号 |
"""

PENDING = """## 下一步

- [ ] **需要戴头显确认**：默认 4.0 m / 4.6 m 下屏幕张角 59.8°×35.8°，
      看是否够大、位置是否舒服（调 `RE6VR_SCREEN_DIST` / `RE6VR_SCREEN_WIDTH`，
      日志会打印新的张角）。
- [ ] 确认画面比例正确（方格测试图离线是方的；头显里应同样是方的）。
- [ ] 若帧率吃紧：先看 `compositor: readback X ms/frame`（真瓶颈），
      再考虑 `RE6VR_SWAPCHAIN_W/H=545/800`。
- [ ] Stage 2：真双视角立体渲染（每眼独立相机，逆向 MT Framework 视图矩阵）。
      侦察工具已就绪并标定，见"Stage 2"一节；**下一步需要跑一次游戏收 cap: 日志**。
- [ ] Stage 3：头部朝向 / 6DoF 姿态注入。

## 本次改动（2026-09-22 下午）

1. **修 `screen_dist` 无效**：`place_panel` 原来把面板放在 `玩家 + screen_dist + 1.6`，
   而 `draw_quad` 又把虚拟相机放在面板前硬编码的 1.6 m。两者自洽，但结果是
   **屏幕看起来多大完全不受 `RE6VR_SCREEN_DIST` 影响**，真实视距恒为 1.6 m。
   现在两处都用 `screen_dist`，虚拟相机正好落在玩家位置。
   日志证据：改前 `screen 4.0 m ahead ... panel at 1.60 m`，改后 `at 4.00 m`。
2. **默认值改为影院档** 4.0 m / 4.6 m（张角 59.8°×35.8°）。
3. **新增真实几何离线自检** `RE6VR_SELFTEST_REAL`，双眼图落盘。
4. **新增运行时每眼 FOV 日志**，作为"屏幕该做多大"的标尺。
5. **修复经典自检的失效探测点**（硬编码 ±0.95 NDC → 按源图 UV 取象限中心）。
6. **纠正眼睛 dump 布局的认知**（结论 7），并补上正确解读它的脚本。
"""


def main():
    path = os.path.join(ROOT, "README.md")
    text = HEAD + "\n" + BODY + "\n" + LESSONS + "\n" + STATUS + "\n" + PENDING
    with io.open(path, "w", encoding="utf-8", newline="\n") as f:
        f.write(text)
    print("wrote %s (%d bytes, %d lines)" % (path, len(text.encode("utf-8")),
                                             text.count("\n") + 1))


if __name__ == "__main__":
    main()
